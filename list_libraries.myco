# List all available libraries using C-style for loop
let libraries = [
    "math", "string", "array", "file", "dir", 
    "maps", "sets", "trees", "graphs", "heaps", 
    "queues", "stacks", "time", "regex", "json", 
    "http", "server", "web", "database", "arduino", "graphics"
];

print("Available Myco Libraries:");
print("========================");
for let i = 0; i < libraries.length; i = i + 1:
    print(libraries[i]);
end
